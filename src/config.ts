/**
 * Default address of the companion downloader server (the Node app in
 * `server/`). It runs on your computer; the phone reaches it over your LAN.
 *
 * This was pre-filled with your computer's current LAN IP. If it changes,
 * update it here or in the app's "Add from link" screen (Server settings).
 */
export const DEFAULT_SERVER_URL = 'http://192.168.1.233:4000';
