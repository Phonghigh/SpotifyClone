/**
 * Copy this file to `src/config.local.ts` (gitignored) and paste in the real
 * API key — the same value you set as the `API_KEY` env var on your server
 * (Render dashboard, or your local `server/.env`).
 */
export const API_KEY = 'REPLACE_WITH_YOUR_API_KEY';

/**
 * Optional: your PC's LAN IP + port for "Local (Wi-Fi)" server mode, e.g.
 * `http://192.168.1.23:4000`. Leave blank to type it in the app instead.
 */
export const LOCAL_SERVER_URL = '';
